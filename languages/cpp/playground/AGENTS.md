## Workflow
- `just build`: compiles the code
- `just test`: runs tests

Be smart about this, depending on the nature of the change compiling is enough/it is more
economical to only compile (as a first step).
